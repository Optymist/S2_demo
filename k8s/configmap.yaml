apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: microservices-demo
  labels:
    app: microservices-demo
data:
  BACKEND_URL: "http://backend"
  NODE_ENV: "production"
  LOG_LEVEL: "info"
