{
  "name": "s2-demo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prepare": "husky install",
    "trivy:scan": "node .github/scripts/trivy-scan.js",
    "security:check": "npm run trivy:scan"
  },
  "devDependencies": {
    "adm-zip": "^0.5.16",
    "eslint": "^8.57.1",
    "husky": "^8.0.3",
    "lint-staged": "^15.5.2",
    "prettier": "^3.6.2",
    "tar": "^6.2.1"
  },
  "lint-staged": {
    "*.js": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{json,md}": [
      "prettier --write"
    ]
  }
}
